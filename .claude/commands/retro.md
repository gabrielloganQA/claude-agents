---
description: Gera retrospectiva consolidada da semana — bugs, PRs, MTTR, hotspots, ações
argument-hint: "[opcional: --days N pra mudar janela (default 7)]"
---

Invoque o subagente **retrospective** para coletar dados via `gh` CLI, calcular métricas e publicar relatório.

Janela padrão: últimos 7 dias. Pode aceitar `--days 14` ou `--days 30`.
